import Helmet from "react-helmet";

const UpdateUser = () => {
    return (
        <div>
            <Helmet>
                <title>Update Profile</title>
            </Helmet>
        </div>
    );
};

export default UpdateUser;